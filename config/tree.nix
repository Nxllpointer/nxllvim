{
  plugins.chadtree = {
    enable = true;
    theme = {
      iconColourSet = "github";
      textColourSet = "nord";
    };
    view.windowOptions = {
      winfixbuf = true; # Only allow tree in window
    };
  };
}
